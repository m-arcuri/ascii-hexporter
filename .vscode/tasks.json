{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "gcc -g hexporter.c -o hexport",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}